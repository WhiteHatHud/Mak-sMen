-- database/migrations/README.md
# Alembic Migrations

This folder contains Alembic configuration and versioned migrations.

Commands (examples):

```bash
alembic upgrade head
alembic downgrade -1
alembic revision -m "feature X"
```